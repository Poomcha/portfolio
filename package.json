{
  "name": "terminal-portfolio-ai",
  "version": "1.0.0",
  "scripts": {
    "deploy": "npm run build --prefix client",
    "install": "npm install --prefix client && npm install --prefix server"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
